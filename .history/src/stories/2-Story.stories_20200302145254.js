import React from 'react';
import { action } from '@storybook/addon-actions';
import { Button } from '@storybook/react/demo';

export default {
  title: 'title',
  component: Text,
};

export const Titre = () => <h1>Hello ceci est un test</h1>;
